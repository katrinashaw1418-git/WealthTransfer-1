This is a very strong design — you’re now at ~institutional-grade. I’ll be precise and critical so you don’t carry hidden risk forward.

✅ Overall Verdict
Gate B + reporting layer: clean, no conflicts
Wealth planner structure: ~90–95% correct
Compliance posture: strong (AUSTRAC + pre-AFSL safe)
Main gaps: a few subtle but important edge cases (versioning integrity, access leakage, and audit completeness)
🔍 What is CORRECT (no change needed)

You got these exactly right:

1. True append-only architecture
risk_profile_snapshots ✅
planner_notes (no updatedAt) ✅
wealth_plan_versions atomic creation ✅

This is audit-safe and regulator defensible

2. Advice containment (VERY important)
adviceType LOCKED = "general" ✅
server-side enforcement ✅
disclaimer stored as full text ✅

This avoids accidental personal advice classification risk

3. Server-side compliance gates
Disclaimer gate (403) ✅
Version mismatch handling ✅
Role enforcement ✅

This is exactly how regulators expect it — UI is irrelevant, API is law

4. Objective supersession model
New row + superseded_at ✅
No deletes ✅

Correct pattern for financial advice audit trails

5. No execution capability
No trade / invest / rebalance paths ✅
No coupling to ledger ✅

This keeps you safely outside AFSL execution scope

⚠️ What MUST be fixed (real risks)

These are not cosmetic — fix before build.

1. ❗ Version integrity is NOT fully protected

Right now:

You say “version created on every write”
But nothing enforces it
Risk:

Developer forgets → silent audit gap

Fix:

Add DB-level + service-level enforcement

// add constraint helper logic
// enforce via service wrapper ONLY — no direct table writes

function writeWithVersioning(tx, planId, action) {
  // perform write
  // then ALWAYS:
  // create wealthPlanVersions row in same transaction
}

AND:

-- optional: trigger (strongly recommended)
AFTER INSERT OR UPDATE ON any planner table
→ assert version row created in same tx
2. ❗ Disclaimer gate missing “scope binding”

Right now:

Ack is tied to wealthPlanId
Risk:

Client acknowledges once → sees future materially different plan

Fix (important):

Bind acknowledgement to plan version

wealthPlanVersionId: integer(...)

Then gate becomes:

client must have acknowledged the CURRENT version
3. ❗ Missing “plan lock during review”

You have:

status: under_review
wealth_plan_reviews

But:

Risk:

Adviser edits plan during compliance review

Fix:
IF status = under_review
→ block:
  - new recommendations
  - new objectives
  - new risk snapshots

Allow only:

notes
compliance actions
4. ❗ selectedModelPortfolio needs structure constraint

You used jsonb (correct), but:

Risk:

Different shapes over time → unreadable history

Fix:

Enforce schema shape:

type ModelPortfolioSnapshot = {
  name: string;
  assetAllocation: Record<string, number>;
  benchmark: string;
  riskLevel: string;
  version: string;
};

Validate before insert.

5. ❗ Missing “recommendation → version binding”

Right now:

recommendation links to risk snapshot ✅
BUT NOT to plan version ❌
Risk:

You cannot reconstruct:

“what recommendation existed in version 3”

Fix:
wealthPlanVersionId: integer(...)
6. ❗ Notes supersession not explicit

You say:

new note supersedes old

But no field enforces it.

Fix:
supersedesNoteId: integer(...)

(optional but cleaner for audit chains)

7. ❗ Admin “read-only” is not technically enforced

You stated:

admin cannot mutate

But unless enforced:

Risk:

Future dev adds admin endpoint → breaks model

Fix:

Centralise write permissions:

assertRole(user, ["adviser"]) // ONLY place writes allowed

No exceptions.

8. ❗ Missing “soft delete prohibition”

You didn’t explicitly block deletes.

Fix:

At DB level:

REVOKE DELETE ON ALL planner tables

Or enforce in ORM.

9. ❗ Audit log missing BEFORE/AFTER state

You log:

action
resource

But not:

before_state
after_state
Risk:

Not regulator-grade

Fix:
metadata: {
  before: {...},
  after: {...}
}
10. ❗ Missing “client acknowledgement of recommendation”

You track:

acknowledged_at

But:

Gap:

No explicit client action record

Fix:

Add:

acknowledgedBy: integer(clientUserId)
➕ What should be ADDED (to future-proof)
1. Plan “freeze snapshot” endpoint
POST /api/adviser/wealth-plans/:id/freeze

Creates:

version
locks plan for review
2. Compliance flag severity
flagSeverity: low | medium | high | critical
3. Recommendation risk alignment check

Before insert:

if recommendation risk > client risk → BLOCK
4. “No financial product reference” guard (optional but smart)

Block wording like:

“buy”
“allocate to specific fund”

Until AFSL

🧠 Final Assessment
You are now building:

✔ Not a fintech toy
✔ Not a CRM
✔ A compliance-grade planning system

✅ Final Build Decision

You can proceed ONLY after fixing these 5 critical items:

1. Version enforcement (hard)
2. Disclaimer bound to plan version
3. Plan lock during review
4. Recommendation → version linkage
5. Audit log before/after state
🚀 After Fixes — Clean Build Path
4. Wealth Planner Schema (with fixes)
5. API + compliance gates
6. UI
7. Verification script
8. HARD STOP