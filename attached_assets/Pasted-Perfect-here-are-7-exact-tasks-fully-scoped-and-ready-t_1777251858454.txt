Perfect — here are **7 exact tasks**, fully scoped and ready to copy into your task system.

---

# 🟢 TASK 1 — Monitoring foundation

```txt
Title:
Implement system + business monitoring

Purpose:
Ensure visibility into system health and wealth-critical signals.

Scope:
- Add /health endpoint (DB + server check)
- Log all 5xx errors (structured logs)
- Track:
  - wallet vs ledger drift count
  - failed transactions
  - audit_log write failures
  - fee deduction failures
- Persist logs (not console-only)

Acceptance criteria:
- /health returns OK + DB status
- Errors stored and queryable
- Metrics visible via logs or dashboard
```

---

# 🟢 TASK 2 — Alerting system

```txt
Title:
Implement alerting for critical system events

Purpose:
Ensure no silent failures in production.

Scope:
- Trigger alerts on:
  - wallet ≠ ledger
  - transaction stuck in pending
  - audit log write failure
  - reconciliation mismatch
- Deliver alerts via:
  - email OR Slack (minimum)
- Rate-limit duplicate alerts

Acceptance criteria:
- Alerts fire on test triggers
- Alerts delivered within seconds
- No alert spam (rate limiting works)
```

---

# 🔴 TASK 3 — Kill switch system (highest priority)

```txt
Title:
Implement global kill switch for money and write operations

Purpose:
Allow instant shutdown of risky operations.

Scope:
- Add env/config flags:
  - DISABLE_TRANSACTIONS
  - DISABLE_DEPOSITS
  - DISABLE_WITHDRAWALS
  - DISABLE_FEE_DEDUCTIONS
- Enforce in all relevant routes/services
- Return controlled error (503 or domain error)
- UI shows "temporarily unavailable"

Acceptance criteria:
- Toggle flag → operations blocked immediately
- No partial writes occur
- UI reflects disabled state
```

---

# 🟢 TASK 4 — Rollback capability

```txt
Title:
Implement deployment + database rollback process

Purpose:
Recover quickly from bad deploys or data issues.

Scope:
- Ensure previous build is redeployable
- Create DB snapshot before deploy
- Document restore steps
- Test rollback scenario:
  - simulate bad deploy
  - rollback code + DB

Acceptance criteria:
- Rollback completes < 5 minutes
- System returns to prior consistent state
```

---

# 🟢 TASK 5 — Security baseline

```txt
Title:
Implement minimum production security controls

Purpose:
Prevent obvious attack vectors before launch.

Scope:
- Require auth for all protected routes
- Hide admin routes from public
- Add rate limiting (login + critical APIs)
- Restrict file uploads (type + size)
- Validate all input fields

Acceptance criteria:
- Unauthorized access blocked
- Rate limits enforced
- Invalid inputs rejected
```

---

# 🟢 TASK 6 — Compliance integrity validation

```txt
Title:
Validate audit, advice, and compliance integrity

Purpose:
Ensure platform meets wealth regulatory expectations.

Scope:
- Verify audit_logs are append-only
- Verify every write has audit entry
- Verify review lock enforcement
- Ensure no fake/synthetic data shown
- Add disclaimer checks where needed

Acceptance criteria:
- Audit rows immutable
- 100% write coverage in audit
- Lock blocks all restricted actions
```

---

# 🟢 TASK 7 — Pre-launch validation gate

```txt
Title:
Enforce pre-launch safety validation gate

Purpose:
Block deployment if system is unsafe.

Scope:
- Use scripts/pre-launch-safety.ts
- Ensure:
  - idempotency checks
  - ledger validation
  - lifecycle correctness
  - reconciliation checks
- Add optional:
  --strict mode (fail on SKIP)

Acceptance criteria:
- Script must PASS before deploy
- CI blocks deploy on FAIL
- Strict mode configurable
```

---

# 🎯 EXECUTION ORDER (important)

```txt
1. Kill switch (Task 3) 🔴
2. Alerting (Task 2)
3. Monitoring (Task 1)
4. Pre-launch gate (Task 7)
5. Rollback (Task 4)
6. Security (Task 5)
7. Compliance validation (Task 6)
```

---

# 🚫 DO NOT ADD

```txt
❌ product features
❌ UI enhancements
❌ portfolio logic changes
❌ payment rails
❌ benchmark personalisation
```

---

# 🧾 FINAL RESULT

After all 7 tasks:

```txt
✔ You can detect issues
✔ You can stop the system
✔ You can recover safely
✔ You can prove compliance
```

---

# 🔥 One-line summary

```txt
This turns your platform from “working” → “production survivable”
```

---

If you want next:
I can simulate a **real incident (e.g. ledger mismatch at 2am)** and walk you through exactly how your new system responds.
