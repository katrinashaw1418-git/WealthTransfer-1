Good — now I’ll give you a **clean, final TO-DO / SKIP / ADD list** based on your **actual board state** (not theory).

---

# ✅ CURRENT STATE (verified)

```txt
Active:
✔ #140 Planner test fixture cleanup
✔ #141 Wallet vs Ledger drift audit

Done:
✔ #132 synthetic data removed
✔ #133 pre-launch script
✔ #135 CI tripwire
✔ audit + lock + diff system
```

👉 This matches what we expected 

---

# 🟢 TO-DO LIST (EXECUTE ONLY THESE)

## 1. Let active tasks finish

```txt
✔ #140 → make tests deterministic
✔ #141 → fix / explain drift + tx 349
```

👉 Do NOT interrupt

---

## 2. After completion — run safety script

```bash
npx tsx scripts/pre-launch-safety.ts
```

Target:

```txt
✔ 0 FAIL
✔ Only valid SKIPs (if any)
```

---

## 3. Verify outcomes (2 min)

```txt
✔ Planner test passes consistently
✔ No review_pending leakage
✔ Wallet = ledger (or fully explained)
✔ Missing receipt resolved
✔ No FK cleanup errors
```

---

# 🟡 ADD LIST (create AFTER current tasks)

## Add 1 — Strict mode (important)

```txt
Title:
Add --strict mode to pre-launch safety script

Purpose:
Fail build if any SKIP exists

Why:
Right now SKIP = PASS
In real money environment → SKIP = RISK
```

---

## Add 2 — Clean demo seed data (optional but good)

```txt
Title:
Clean or isolate demo seed wallet data (user 1)

Purpose:
Remove misleading drift alerts from demo users

Approach:
- delete OR flag as demo
- OR exclude from reconciliation
```

---

# 🔴 SKIP LIST (DO NOT TOUCH NOW)

These are distractions at this stage:

```txt
❌ Personalised AI benchmark
❌ Payout batch system
❌ Payment rails
❌ Commission engine
❌ UX enhancements
❌ Audit UI improvements
❌ CSV exports / reporting
❌ Notification features
```

👉 All of these are **post-launch or growth features**

---

# 🧠 IMPORTANT CLARITY

Your current work is:

```txt
Integrity phase (NOT feature phase)
```

Meaning:

```txt
✔ Make system truthful
✔ Make tests deterministic
✔ Remove misleading data
✔ Ensure audit reliability
```

NOT:

```txt
❌ Add functionality
❌ Improve UX
❌ Expand features
```

---

# 🎯 FINAL EXECUTION SEQUENCE

```txt
Step 1 → finish #140 + #141
Step 2 → run pre-launch script
Step 3 → confirm zero FAIL
Step 4 → add strict mode
Step 5 → optional demo data cleanup
```

---

# 🔥 FINAL VERDICT

```txt
You are VERY close to launch-ready

Only blockers left:
✔ test determinism
✔ data truth (drift + receipt)
```

---

# 🧾 ONE-LINE ANSWER

```txt
Finish current 2 tasks → run safety script → add strict mode → ignore everything else
```

---

If you want next:
👉 I can give you a **go-live checklist (infra, monitoring, kill switch, rollback)**
👉 or simulate a **real production failure scenario (what happens if something breaks)**
