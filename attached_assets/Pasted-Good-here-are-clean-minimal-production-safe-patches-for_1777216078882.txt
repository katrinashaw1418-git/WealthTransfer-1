Good — here are **clean, minimal, production-safe patches** for both **#92 and #94**, aligned to your *actual architecture* (no duplicate planner system).

---

# 🧩 Task #92 — Gate B script fix (JWT env)

## ✅ Patch 1 — bootstrap env BEFORE imports

**File:** `scripts/test-fee-deduction-gate-b.ts`

```ts
// 🔴 MUST be first lines — before ANY other imports
import "dotenv/config";

// ensure test-safe defaults (do NOT override if already set)
process.env.NODE_ENV ||= "test";
process.env.JWT_SECRET ||= "test-jwt-secret";

// optional: make failures obvious
if (!process.env.JWT_SECRET) {
  throw new Error("JWT_SECRET not set");
}
```

---

## ✅ Patch 2 — if auth helper reads env at import-time

If you have something like:

```ts
// auth.ts
const SECRET = process.env.JWT_SECRET!;
```

👉 make it lazy:

```ts
export function getJwtSecret(): string {
  if (!process.env.JWT_SECRET) {
    throw new Error("JWT_SECRET missing");
  }
  return process.env.JWT_SECRET;
}
```

And update usage:

```ts
jwt.sign(payload, getJwtSecret());
```

---

## ✅ Patch 3 — tsx execution (optional but robust)

If CI still fails:

```bash
NODE_ENV=test JWT_SECRET=test-jwt-secret npx tsx scripts/test-fee-deduction-gate-b.ts
```

---

## ✅ Expected result

```txt
ALL GATE B FEE DEDUCTION TESTS PASSED ✅
```

👉 Only then mark **#92 COMPLETE**

---

# 🧱 Task #94 — Correct schema (NO duplicate planner system)

👉 This is **additive only** to your existing `adviceRecords` system

---

## ✅ Patch 4 — `clientObjectives`

**File:** `shared/schema.ts`

```ts
export const clientObjectives = pgTable("client_objectives", {
  id: serial("id").primaryKey(),

  clientUserId: integer("client_user_id")
    .references(() => users.id)
    .notNull(),

  adviceRecordId: integer("advice_record_id")
    .references(() => adviceRecords.id)
    .notNull(),

  objectiveType: text("objective_type").notNull(),
  // retirement | income | growth | capital_preservation | other

  description: text("description").notNull(),

  targetAmount: decimal("target_amount", { precision: 15, scale: 2 }),
  targetDate: date("target_date"),

  priority: text("priority").notNull().default("primary"),
  // primary | secondary | aspirational

  createdBy: integer("created_by")
    .references(() => users.id)
    .notNull(),

  createdAt: timestamp("created_at").defaultNow(),

  supersededAt: timestamp("superseded_at"),
  supersededBy: integer("superseded_by"),
});
```

---

## ✅ Patch 5 — `clientDocuments`

```ts
export const clientDocuments = pgTable("client_documents", {
  id: serial("id").primaryKey(),

  clientUserId: integer("client_user_id")
    .references(() => users.id)
    .notNull(),

  adviceRecordId: integer("advice_record_id")
    .references(() => adviceRecords.id),

  documentType: text("document_type").notNull(),
  // fact_find | risk_profile | soa | correspondence | other

  fileName: text("file_name").notNull(),
  storageKey: text("storage_key").notNull(),

  fileSizeBytes: integer("file_size_bytes"),
  mimeType: text("mime_type"),

  uploadedBy: integer("uploaded_by")
    .references(() => users.id)
    .notNull(),

  uploadedAt: timestamp("uploaded_at").defaultNow(),
});
```

---

## ✅ Patch 6 — `adviserNotes` (append-only + chain)

```ts
export const adviserNotes = pgTable("adviser_notes", {
  id: serial("id").primaryKey(),

  adviceRecordId: integer("advice_record_id")
    .references(() => adviceRecords.id)
    .notNull(),

  previousNoteId: integer("previous_note_id"),

  noteText: text("note_text").notNull(),

  noteType: text("note_type").notNull().default("general"),
  // general | compliance | follow_up | instruction

  visibility: text("visibility").notNull().default("adviser_only"),
  // adviser_only | shared_with_client | admin_only

  createdBy: integer("created_by")
    .references(() => users.id)
    .notNull(),

  createdAt: timestamp("created_at").defaultNow(),

  // ❌ NO updatedAt — append-only enforced
});
```

---

## ✅ Patch 7 — `adviceRecordVersions` (REAL versioning layer)

```ts
export const adviceRecordVersions = pgTable("advice_record_versions", {
  id: serial("id").primaryKey(),

  adviceRecordId: integer("advice_record_id")
    .references(() => adviceRecords.id)
    .notNull(),

  versionNumber: integer("version_number").notNull(),

  snapshotData: jsonb("snapshot_data").notNull(),

  changeReason: text("change_reason").notNull(),

  createdBy: integer("created_by")
    .references(() => users.id)
    .notNull(),

  createdAt: timestamp("created_at").defaultNow(),
});
```

---

# 🔐 Patch 8 — audit log BEFORE / AFTER

**File:** `services/audit.ts` (or equivalent)

```ts
export async function writeAuditLog(tx: any, params: {
  userId: number;
  action: string;
  resourceType: string;
  resourceId: number;
  before: any;
  after: any;
}) {
  await tx.insert(auditLogs).values({
    userId: params.userId,
    action: params.action,
    resourceType: params.resourceType,
    resourceId: params.resourceId,
    metadata: {
      before: params.before,
      after: params.after,
    },
    createdAt: new Date(),
  });
}
```

---

# 🔒 Patch 9 — review lock enforcement

**Where:** advice write routes

```ts
function assertNotUnderReview(advice: any) {
  if (advice.status === "review_pending") {
    throw new Error("RECORD_LOCKED_UNDER_REVIEW");
  }
}
```

Apply to:

* update advice
* modify allocation
* update recommendation fields

---

# 🧠 Patch 10 — version wrapper (critical)

```ts
export async function writeWithVersioning(
  db: any,
  adviceRecordId: number,
  userId: number,
  changeReason: string,
  writeFn: (tx: any) => Promise<void>
) {
  await db.transaction(async (tx: any) => {
    const before = await tx.query.adviceRecords.findFirst({
      where: eq(adviceRecords.id, adviceRecordId),
    });

    await writeFn(tx);

    const after = await tx.query.adviceRecords.findFirst({
      where: eq(adviceRecords.id, adviceRecordId),
    });

    const lastVersion = await tx.query.adviceRecordVersions.findFirst({
      where: eq(adviceRecordVersions.adviceRecordId, adviceRecordId),
      orderBy: desc(adviceRecordVersions.versionNumber),
    });

    const nextVersion = (lastVersion?.versionNumber ?? 0) + 1;

    await tx.insert(adviceRecordVersions).values({
      adviceRecordId,
      versionNumber: nextVersion,
      snapshotData: after,
      changeReason,
      createdBy: userId,
    });

    await writeAuditLog(tx, {
      userId,
      action: changeReason,
      resourceType: "advice_record",
      resourceId: adviceRecordId,
      before,
      after,
    });
  });
}
```

---

# 🚫 Explicit NON-BUILD rules

```txt
DO NOT CREATE:
- wealthPlans
- wealthPlanRecommendations
- riskProfileSnapshots
- adviceDisclaimerAcknowledgements
- wealthPlanVersions

DO NOT:
- duplicate adviceRecords
- create planner execution layer
- connect to ledger
- mutate wallet.balance
```

---

# ✅ Final Execution

```txt
1. Apply Task #92 patches → PASS tests
2. Apply Task #94 schema patches
3. Add audit + version wrapper
4. Add review lock
5. Build UI on adviceRecords
```

---

If you want next:

👉 I can give you **API routes + exact controller code for #94** so dev doesn’t drift or overbuild.
