Let me do a full cross-check of everything in this conversation against the task board state before giving you a complete build sequence.
  ::view-transition-group(*),
  ::view-transition-old(*),
  ::view-transition-new(*) {
    animation-duration: 0.25s;
    animation-timing-function: cubic-bezier(0.19, 1, 0.22, 1);
  }
VvisualizeVvisualize show_widgetCross-check verdict: the build state is clean. Here is a precise account of what is verified, what is outstanding, and what four later-phase areas have not been designed yet.
What is fully verified and done. Auth, ledger, fee engine (#11–68, #92–93), Phase 2.2/2.3 advice layer, and #94 wealth planner extensions are all shipped and consistent with each other. No duplicate systems, no conflicting sources of truth.
What is in flight right now. The five accepted tasks, in strict build order: audit log standardisation must land first because every subsequent write task inherits the standard automatically. Review lock enforcement goes second. Then planner UI, document storage, and SOA snapshot hook in parallel.
What is outstanding but not yet designed. Four areas need design work before they can be specced, gated, or built:
The payout batch system needs a one-page scope document before you accept it. The only question is whether it generates payment files or initiates transfers. If it generates files only, it is an extension of the existing reporting layer and can be gated lightly. If it initiates transfers, it is a new money-movement system that needs its own gate, verification script, and compliance sign-off — same rigour as Gate B.
The client-facing portal is implied by the /wealth/plan route in the planner UI but has no auth design, session model, or notification delivery system defined. The planner UI task will create the read-only plan view, but a real client portal needs its own design before it is built as a standalone surface.
Commission tiers and adviser billing have never been specced. The fee engine handles deductions and reversals, but structured billing periods, invoices, and commission tier logic do not exist yet.
Regulatory reporting exports — ASIC, AUSTRAC, tax reporting — have not been designed. The data is in the ledger and advice records, but no export schema or delivery mechanism exists.
The one thing to do before anything else. Check the planner verification script is on the task board. The 38 accepted operational tasks are in the queue, but the planner verification script — testing review lock, audit log before/after, append-only notes, and no ledger side effects from planner writes — was specified in this conversation but does not appear as an accepted task in the board. Add it before the planner UI ships.You said: This is now clean, consistent, and execution-ready.This is now clean, consistent, and execution-ready. I’ll be precise—no fluff.
✅ Final Cross-Check Verdict
✔ What is correct#92, #93, #94: properly closed and alignedNo duplicate planner system: confirmedAdvice vs ledger separation: intactBuild order: correctHard stop on payout execution: correctSOA → version snapshot hook: correctPlanner sits on adviceRecords: correct
👉 There are no architectural conflicts.
⚠️ Only ONE gap remains (still true)
You have not formally moved “Planner verification script” into the active build sequence.
Right now it sits in:NEXT PHASE → after current sprint shipsThat is too late.
🔴 Required correction (small but important)
Move:Planner verification scriptFrom:NEXT PHASETo:NOW IN PROGRESS (after Step 5)✅ Correct final build sequence#92 ✅
#93 ✅
#94 ✅

1. Audit log standardisation
2. Review lock enforcement
3. Planner UI
4. Document storage
5. SOA snapshot hook
6. Planner verification script  ← MOVE HERE🧠 Why this matters (subtle but critical)
If you don’t do this:Planner UI ships ❌Bugs exist in:
 audit logsreview lock gapsversion snapshotsYou discover later ❌ (harder to fix)
If you DO move it:You validate:
 append-only notesaudit before/afterlock enforcementzero ledger side effectsBefore exposure ✅
🟢 Everything else is correct
✔ Good decisionsPayout batch → scope gate first ✔Operational polish separated ✔External payments gated ✔Commission / reporting deferred ✔AFSL execution isolated ✔
🔥 Final Answer (very simple)
👉 Your system is ready to build 👉 Your sequencing is correct 👉 Your risk controls are intact
Only change:Move planner verification script into current build, not next phaseIf you want next step:
👉 I’ll write the exact verification script (copy-paste test file) so your dev can run it immediately after Step 5. verify and cross checkShow more6:37 AMClaude responded: Confirmed.Confirmed. The correction is correct and the reasoning is sound.
Cross-check verdict: accurate on all points. One minor precision addition.
The verification script placement fix is right. Shipping planner UI without a passing verification script is the same mistake that was caught with Gate B — the script is not a post-ship audit, it is the proof that the build is safe to expose. It belongs at step 6 in the current sprint, not in the next phase.
Everything else in the final cross-check is verified clean. No conflicts, no contradictions.
One precision addition the review does not mention: the verification script needs to be written before step 3 (planner UI), not after step 5. The reason is the same as the audit log argument — developers building the UI need to know what the script will test so they build to pass it, not discover failures after the fact. Write the script at step 1, run it at step 6.

Here is the exact verification script, ready to hand to your developer.
Create scripts/test-planner.ts
tsimport { db } from "../server/db";
import { eq, desc } from "drizzle-orm";
import {
  users,
  adviceRecords,
  adviceRecordVersions,
  adviserNotes,
  clientObjectives,
  clientDocuments,
  auditLogs,
  ledgerEntries,
} from "../shared/schema";

type TestResult = { name: string; passed: boolean; details?: string };
const results: TestResult[] = [];

function pass(name: string, details?: string) {
  results.push({ name, passed: true, details });
}
function fail(name: string, details?: string) {
  results.push({ name, passed: false, details });
}

async function expectStatus(
  res: Response,
  expected: number,
  testName: string
) {
  if (res.status === expected) {
    pass(testName);
  } else {
    fail(testName, `expected ${expected}, got ${res.status}`);
  }
}

const BASE = "http://localhost:5000";

async function asRole(
  role: "admin" | "adviser" | "second_adviser" | "client",
  path: string,
  method = "GET",
  body?: object
): Promise<Response> {
  const tokens: Record<string, string> = (global as any).__testTokens__;
  return fetch(`${BASE}${path}`, {
    method,
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${tokens[role]}`,
    },
    body: body ? JSON.stringify(body) : undefined,
  });
}

async function run() {

  // ── Seed ────────────────────────────────────────────────────────────────────
  // Create deterministic test users, advice record, and initial state.
  // Seeds are isolated — use a test-specific email prefix to avoid collisions.

  const seed = Date.now();
  const [admin, adviser, secondAdviser, client] = await Promise.all([
    db.query.users.findFirst({ where: eq(users.email, `test-admin-${seed}@test.local`) }),
    // ... full seed logic per your existing test pattern
  ]);

  // Store auth tokens globally so asRole() can pick them up
  // (same pattern as scripts/test-fee-deduction-gate-b.ts)

  let adviceRecordId: number;
  let versionCountBefore: number;

  // ── 1. Role access ───────────────────────────────────────────────────────────

  // 1a. Non-adviser cannot create an advice record
  const res1a = await asRole("client", "/api/adviser/advice-records", "POST", {
    clientUserId: client!.id,
    strategySummary: "test",
  });
  await expectStatus(res1a, 403, "non-adviser cannot create advice record");

  // 1b. Adviser cannot access another adviser's client record
  const res1b = await asRole("second_adviser", `/api/adviser/advice-records/${adviceRecordId}`, "GET");
  await expectStatus(res1b, 403, "adviser blocked from another adviser's record");

  // 1c. Client cannot write to any advice table
  const res1c = await asRole("client", `/api/adviser/advice-records/${adviceRecordId}/notes`, "POST", {
    noteText: "client trying to write",
    noteType: "general",
    visibility: "adviser_only",
  });
  await expectStatus(res1c, 403, "client cannot write to advice tables");

  // 1d. Admin can read record but cannot mutate it
  const res1d = await asRole("admin", `/api/adviser/advice-records/${adviceRecordId}`, "POST", {
    strategySummary: "admin trying to mutate",
  });
  if (res1d.status === 403 || res1d.status === 405) {
    pass("admin cannot mutate advice record");
  } else {
    fail("admin cannot mutate advice record", `got ${res1d.status}`);
  }

  // ── 2. Review lock ────────────────────────────────────────────────────────────

  // Set record to review_pending
  await db.update(adviceRecords)
    .set({ status: "review_pending" })
    .where(eq(adviceRecords.id, adviceRecordId));

  // 2a. POST to objectives blocked during review
  const res2a = await asRole("adviser", `/api/adviser/advice-records/${adviceRecordId}/objectives`, "POST", {
    objectiveType: "retirement",
    description: "blocked write",
    priority: "primary",
  });
  await expectStatus(res2a, 423, "objectives blocked during review_pending");

  // 2b. POST to recommendations blocked during review
  const res2b = await asRole("adviser", `/api/adviser/advice-records/${adviceRecordId}/recommendations`, "POST", {
    strategySummary: "blocked",
  });
  await expectStatus(res2b, 423, "recommendations blocked during review_pending");

  // 2c. Notes still allowed during review
  const res2c = await asRole("adviser", `/api/adviser/advice-records/${adviceRecordId}/notes`, "POST", {
    noteText: "compliance note during review",
    noteType: "compliance",
    visibility: "adviser_only",
  });
  await expectStatus(res2c, 200, "notes allowed during review_pending");

  // Restore to active for remaining tests
  await db.update(adviceRecords)
    .set({ status: "active" })
    .where(eq(adviceRecords.id, adviceRecordId));

  // ── 3. Versioning ─────────────────────────────────────────────────────────────

  // 3a. Every plan write creates a new version row atomically
  versionCountBefore = (await db.query.adviceRecordVersions.findMany({
    where: eq(adviceRecordVersions.adviceRecordId, adviceRecordId),
  })).length;

  const res3a = await asRole("adviser", `/api/adviser/advice-records/${adviceRecordId}/objectives`, "POST", {
    objectiveType: "retirement",
    description: "test objective",
    priority: "primary",
  });
  await expectStatus(res3a, 200, "objective POST succeeds on active record");

  const versionCountAfter = (await db.query.adviceRecordVersions.findMany({
    where: eq(adviceRecordVersions.adviceRecordId, adviceRecordId),
  })).length;

  if (versionCountAfter === versionCountBefore + 1) {
    pass("objective write creates version snapshot atomically");
  } else {
    fail("objective write creates version snapshot atomically",
      `versions before: ${versionCountBefore}, after: ${versionCountAfter}`);
  }

  // 3b. SOA issuance creates version snapshot
  const res3b = await asRole("adviser", `/api/adviser/advice-records/${adviceRecordId}/issue-soa`, "POST");
  const versionCountSOA = (await db.query.adviceRecordVersions.findMany({
    where: eq(adviceRecordVersions.adviceRecordId, adviceRecordId),
  })).length;

  if (versionCountSOA > versionCountAfter) {
    pass("SOA issuance creates version snapshot");
  } else {
    fail("SOA issuance creates version snapshot", "no new version row after SOA issuance");
  }

  // ── 4. Append-only enforcement ────────────────────────────────────────────────

  // 4a. PUT on adviserNotes rejected
  const noteRes = await asRole("adviser", `/api/adviser/advice-records/${adviceRecordId}/notes`, "POST", {
    noteText: "original note",
    noteType: "general",
    visibility: "adviser_only",
  });
  const note = await noteRes.json();

  const res4a = await asRole("adviser", `/api/adviser/notes/${note.id}`, "PUT", {
    noteText: "trying to edit",
  });
  if (res4a.status === 405 || res4a.status === 404) {
    pass("PUT on adviserNotes rejected — append-only enforced");
  } else {
    fail("PUT on adviserNotes rejected", `got ${res4a.status}`);
  }

  // 4b. DELETE on adviserNotes rejected
  const res4b = await asRole("adviser", `/api/adviser/notes/${note.id}`, "DELETE");
  if (res4b.status === 405 || res4b.status === 404) {
    pass("DELETE on adviserNotes rejected");
  } else {
    fail("DELETE on adviserNotes rejected", `got ${res4b.status}`);
  }

  // 4c. adviserNotes table has no updatedAt column (schema inspection)
  try {
    const col = await db.execute(
      `SELECT column_name FROM information_schema.columns
       WHERE table_name = 'adviser_notes' AND column_name = 'updated_at'`
    );
    if ((col.rows ?? col).length === 0) {
      pass("adviserNotes has no updatedAt column");
    } else {
      fail("adviserNotes has no updatedAt column", "updatedAt column exists — append-only violated");
    }
  } catch (e) {
    fail("adviserNotes schema check", String(e));
  }

  // 4d. previousNoteId chain is traversable
  const note2Res = await asRole("adviser", `/api/adviser/advice-records/${adviceRecordId}/notes`, "POST", {
    noteText: "superseding note",
    noteType: "general",
    visibility: "adviser_only",
    previousNoteId: note.id,
  });
  const note2 = await note2Res.json();

  if (note2.previousNoteId === note.id) {
    pass("previousNoteId chain is traversable");
  } else {
    fail("previousNoteId chain is traversable", `previousNoteId is ${note2.previousNoteId}`);
  }

  // 4e. Objective supersession: new row + supersededAt set on prior, atomic
  const obj1Res = await asRole("adviser", `/api/adviser/advice-records/${adviceRecordId}/objectives`, "POST", {
    objectiveType: "education",
    description: "original",
    priority: "secondary",
  });
  const obj1 = await obj1Res.json();

  await asRole("adviser", `/api/adviser/advice-records/${adviceRecordId}/objectives`, "POST", {
    objectiveType: "education",
    description: "updated",
    priority: "secondary",
    supersedes: obj1.id,
  });

  const obj1Check = await db.query.clientObjectives.findFirst({
    where: eq(clientObjectives.id, obj1.id),
  });

  if (obj1Check?.supersededAt !== null) {
    pass("objective supersession sets supersededAt on prior row");
  } else {
    fail("objective supersession sets supersededAt on prior row", "supersededAt is null");
  }

  // ── 5. Audit log before/after ──────────────────────────────────────────────────

  // 5a. Every plan write produces audit log entry with before + after state
  const recentLogs = await db.query.auditLogs.findMany({
    where: eq(auditLogs.resourceId, String(adviceRecordId)),
    orderBy: desc(auditLogs.createdAt),
    limit: 5,
  });

  const logsWithBeforeAfter = recentLogs.filter(
    (l) => l.metadata &&
      typeof l.metadata === "object" &&
      "before" in l.metadata &&
      "after" in l.metadata
  );

  if (logsWithBeforeAfter.length > 0) {
    pass("plan writes produce audit logs with before/after state");
  } else {
    fail("plan writes produce audit logs with before/after state",
      `${recentLogs.length} logs found, none have before/after`);
  }

  // ── 6. No ledger side effects ────────────────────────────────────────────────

  // 6a. No ledger entries created by any planner write
  const ledgerCountBefore = (await db.query.ledgerEntries.findMany({
    where: eq(ledgerEntries.referenceType, "advice_record"),
  })).length;

  await asRole("adviser", `/api/adviser/advice-records/${adviceRecordId}/notes`, "POST", {
    noteText: "ledger check note",
    noteType: "general",
    visibility: "adviser_only",
  });

  const ledgerCountAfter = (await db.query.ledgerEntries.findMany({
    where: eq(ledgerEntries.referenceType, "advice_record"),
  })).length;

  if (ledgerCountAfter === ledgerCountBefore) {
    pass("no ledger entries created by planner writes");
  } else {
    fail("no ledger entries created by planner writes",
      `ledger count before: ${ledgerCountBefore}, after: ${ledgerCountAfter}`);
  }

  // 6b. No wallet.balance mutation from planner writes
  const clientWalletBefore = await db.query.wallets.findFirst({
    where: eq(wallets.userId, client!.id),
  });

  await asRole("adviser", `/api/adviser/advice-records/${adviceRecordId}/objectives`, "POST", {
    objectiveType: "property",
    description: "wallet check objective",
    priority: "aspirational",
  });

  const clientWalletAfter = await db.query.wallets.findFirst({
    where: eq(wallets.userId, client!.id),
  });

  if (clientWalletBefore?.balance === clientWalletAfter?.balance) {
    pass("wallet balance not mutated by planner writes");
  } else {
    fail("wallet balance not mutated by planner writes",
      `before: ${clientWalletBefore?.balance}, after: ${clientWalletAfter?.balance}`);
  }

  // ── 7. Client document record ─────────────────────────────────────────────────

  // 7a. Document upload creates audit record (no file content check — storage is separate)
  const res7a = await asRole("adviser", `/api/adviser/advice-records/${adviceRecordId}/documents`, "POST", {
    documentType: "fact_find",
    fileName: "test-fact-find.pdf",
    storageKey: "test/fact-find-placeholder",
    description: "test upload",
  });
  await expectStatus(res7a, 200, "document record created by adviser");

  // 7b. Client cannot upload documents
  const res7b = await asRole("client", `/api/adviser/advice-records/${adviceRecordId}/documents`, "POST", {
    documentType: "correspondence",
    fileName: "client-upload.pdf",
    storageKey: "test/client-upload",
  });
  await expectStatus(res7b, 403, "client cannot upload documents");

  // ── Output ────────────────────────────────────────────────────────────────────

  console.log("\n── Planner verification results ──\n");
  for (const r of results) {
    console.log(`${r.passed ? "PASS" : "FAIL"} ${r.name}${r.details ? ` — ${r.details}` : ""}`);
  }

  const failed = results.filter((r) => !r.passed);
  if (failed.length > 0) {
    console.error(`\n${failed.length} planner test(s) failed.`);
    process.exit(1);
  }

  console.log("\nALL PLANNER TESTS PASSED ✅");
}

run().catch((err) => {
  console.error("Planner verification script crashed:", err);
  process.exit(1);
});
Required final output:
PASS  non-adviser cannot create advice record
PASS  adviser blocked from another adviser's record
PASS  client cannot write to advice tables
PASS  admin cannot mutate advice record
PASS  objectives blocked during review_pending
PASS  recommendations blocked during review_pending
PASS  notes allowed during review_pending
PASS  objective write creates version snapshot atomically
PASS  SOA issuance creates version snapshot
PASS  PUT on adviserNotes rejected — append-only enforced
PASS  DELETE on adviserNotes rejected
PASS  adviserNotes has no updatedAt column
PASS  previousNoteId chain is traversable
PASS  objective supersession sets supersededAt on prior row
PASS  plan writes produce audit logs with before/after state
PASS  no ledger entries created by planner writes
PASS  wallet balance not mutated by planner writes
PASS  document record created by adviser
PASS  client cannot upload documents

ALL PLANNER TESTS PASSED ✅
Three instructions for the developer before running this:
The seed block needs to follow the same pattern as scripts/test-fee-deduction-gate-b.ts — create isolated test users with a timestamp prefix, store their auth tokens in global.__testTokens__, and clean up after the run. The existing Gate B script has this pattern already; copy it.
The issue-soa route in test 3b must be the actual SOA issuance endpoint path in your codebase. If the route is named differently, update the path — the test logic is correct regardless of the route name.
Add this to package.json scripts so CI can run it:
json"test:planner": "tsx scripts/test-planner.ts"
And add it to the CI pipeline immediately after the planner UI deploys, before the next sprint begins.You said: Verified.Verified. The correction is right: write the planner verification script before the Planner UI build, then run it after SOA/doc/UI work lands.
One fix to the script: it references wallets but doesn’t import it. Add wallets to the schema imports.
Also check these before handing to dev:- Replace /issue-soa with the actual SOA issuance route
- Use the same token/seed pattern as test-fee-deduction-gate-b.ts
- Confirm auditLogs.resourceId type: string vs number
- Use the actual route names for objectives, notes, documents
- Add "test:planner": "tsx scripts/test-planner.ts"Final sequence:1. Write planner verification script now
2. Build audit log standardisation
3. Build review lock enforcement
4. Build Planner UI + document storage + SOA snapshot hook
5. Run planner verification script
6. Do not proceed unless it exits 0This is now safe and complete. verify and cross checkShow more6:39 AMClaude responded: Verified and confirmed.Verified and confirmed. Every point is correct. Here is the precise cross-check before handing to dev.

All five pre-handoff checks — verified:
1. Missing wallets import — confirmed. The script references wallets in test 6b but the import block only lists users, adviceRecords, adviceRecordVersions, adviserNotes, clientObjectives, clientDocuments, auditLogs, ledgerEntries. Add wallets to the destructured import from ../shared/schema.
2. Replace /issue-soa — confirmed. This is a placeholder. The developer must look up the actual SOA issuance route in the existing routes files before running the script. If the route does not exist yet (it is being added by the SOA snapshot hook task), the test should be written but skipped with a // TODO: enable after SOA hook task ships comment so the script still exits 0 during UI development.
3. Token/seed pattern — confirmed. Copy the seed block from scripts/test-fee-deduction-gate-b.ts exactly. Do not write a new auth pattern. Consistency here matters because the Gate B script has already been debugged (including the JWT env fix) and is known to work.
4. auditLogs.resourceId type — this needs a one-line check before the script is run. Look at the auditLogs table definition in shared/schema.ts. If resourceId is integer, the filter eq(auditLogs.resourceId, String(adviceRecordId)) will silently return zero rows and test 5a will false-pass. The correct form depends on the column type:
ts// If resourceId is text/varchar:
eq(auditLogs.resourceId, String(adviceRecordId))

// If resourceId is integer:
eq(auditLogs.resourceId, adviceRecordId)
Check the schema, then use the correct form. Do not guess.
5. Actual route names — confirmed. The script uses /api/adviser/advice-records/:id/objectives, /notes, /documents as placeholders matching the spec. The developer must verify these against the actual route definitions in the server before running. If any route name differs, the test will get a 404 and fail with a misleading error.

Here is the corrected final script with all fixes applied.
scripts/test-planner.ts — corrected, dev-ready:
ts// Run with: npx tsx scripts/test-planner.ts
// Requires: server running on localhost:5000
// Pattern: mirrors scripts/test-fee-deduction-gate-b.ts exactly

import "dotenv/config"; // must be first — loads JWT_SECRET before any other import
import { db } from "../server/db";
import { eq, desc } from "drizzle-orm";
import {
  users,
  adviceRecords,
  adviceRecordVersions,
  adviserNotes,
  clientObjectives,
  clientDocuments,
  auditLogs,
  ledgerEntries,
  wallets, // fix: was missing in original
} from "../shared/schema";

// ── Types ────────────────────────────────────────────────────────────────────

type TestResult = { name: string; passed: boolean; details?: string };
const results: TestResult[] = [];

function pass(name: string, details?: string) {
  results.push({ name, passed: true, details });
}
function fail(name: string, details?: string) {
  results.push({ name, passed: false, details });
}

// ── Auth ─────────────────────────────────────────────────────────────────────
// Copy seed + token pattern exactly from scripts/test-fee-deduction-gate-b.ts.
// Do not write a new auth pattern. Reuse what is already debugged and working.

const BASE = "http://localhost:5000";
const tokens: Record<string, string> = {};

async function login(email: string, password: string): Promise<string> {
  const res = await fetch(`${BASE}/api/auth/login`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ email, password }),
  });
  const data = await res.json();
  if (!data.token) throw new Error(`Login failed for ${email}: ${JSON.stringify(data)}`);
  return data.token;
}

async function asRole(
  role: "admin" | "adviser" | "second_adviser" | "client",
  path: string,
  method = "GET",
  body?: object
): Promise<Response> {
  return fetch(`${BASE}${path}`, {
    method,
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${tokens[role]}`,
    },
    body: body ? JSON.stringify(body) : undefined,
  });
}

async function expectStatus(
  res: Response,
  expected: number,
  testName: string
) {
  if (res.status === expected) {
    pass(testName);
  } else {
    const body = await res.text().catch(() => "");
    fail(testName, `expected ${expected}, got ${res.status}. body: ${body.slice(0, 120)}`);
  }
}

// ── Main ─────────────────────────────────────────────────────────────────────

async function run() {

  // ── Seed ──────────────────────────────────────────────────────────────────
  // Use timestamp prefix for isolation — same pattern as Gate B script.
  // Replace the user creation calls with whatever pattern your Gate B script uses.

  const seed = Date.now();
  const adminEmail    = `test-admin-${seed}@test.local`;
  const adviserEmail  = `test-adviser-${seed}@test.local`;
  const adviser2Email = `test-adviser2-${seed}@test.local`;
  const clientEmail   = `test-client-${seed}@test.local`;
  const password      = "Test1234!";

  // Create users — replace with your project's user creation helper
  // e.g. await createTestUser({ email: adminEmail, role: "admin", password })
  // This must match exactly how test-fee-deduction-gate-b.ts creates users.

  tokens["admin"]          = await login(adminEmail, password);
  tokens["adviser"]        = await login(adviserEmail, password);
  tokens["second_adviser"] = await login(adviser2Email, password);
  tokens["client"]         = await login(clientEmail, password);

  // Create an advice record owned by adviser + client
  // Store the id for use in all tests below
  let adviceRecordId: number;
  // e.g. adviceRecordId = (await createTestAdviceRecord(adviserUser, clientUser)).id

  // ── 1. Role access ─────────────────────────────────────────────────────────

  const res1a = await asRole("client", "/api/adviser/advice-records", "POST", {
    clientUserId: /* clientUser.id */ 0,
    strategySummary: "client trying to create",
  });
  await expectStatus(res1a, 403, "non-adviser cannot create advice record");

  const res1b = await asRole(
    "second_adviser",
    `/api/adviser/advice-records/${adviceRecordId!}`,
    "GET"
  );
  await expectStatus(res1b, 403, "adviser blocked from another adviser's record");

  const res1c = await asRole(
    "client",
    `/api/adviser/advice-records/${adviceRecordId!}/notes`,
    "POST",
    { noteText: "client write attempt", noteType: "general", visibility: "adviser_only" }
  );
  await expectStatus(res1c, 403, "client cannot write to advice tables");

  const res1d = await asRole(
    "admin",
    `/api/adviser/advice-records/${adviceRecordId!}`,
    "POST",
    { strategySummary: "admin mutation attempt" }
  );
  if (res1d.status === 403 || res1d.status === 405) {
    pass("admin cannot mutate advice record");
  } else {
    fail("admin cannot mutate advice record", `got ${res1d.status}`);
  }

  // ── 2. Review lock ─────────────────────────────────────────────────────────

  await db
    .update(adviceRecords)
    .set({ status: "review_pending" })
    .where(eq(adviceRecords.id, adviceRecordId!));

  const res2a = await asRole(
    "adviser",
    `/api/adviser/advice-records/${adviceRecordId!}/objectives`,
    "POST",
    { objectiveType: "retirement", description: "blocked write", priority: "primary" }
  );
  await expectStatus(res2a, 423, "objectives blocked during review_pending");

  const res2b = await asRole(
    "adviser",
    `/api/adviser/advice-records/${adviceRecordId!}/recommendations`,
    "POST",
    { strategySummary: "blocked" }
  );
  await expectStatus(res2b, 423, "recommendations blocked during review_pending");

  const res2c = await asRole(
    "adviser",
    `/api/adviser/advice-records/${adviceRecordId!}/notes`,
    "POST",
    { noteText: "compliance note during review", noteType: "compliance", visibility: "adviser_only" }
  );
  await expectStatus(res2c, 200, "notes allowed during review_pending");

  await db
    .update(adviceRecords)
    .set({ status: "active" })
    .where(eq(adviceRecords.id, adviceRecordId!));

  // ── 3. Versioning ──────────────────────────────────────────────────────────

  const versionsBefore = await db.query.adviceRecordVersions.findMany({
    where: eq(adviceRecordVersions.adviceRecordId, adviceRecordId!),
  });

  const res3a = await asRole(
    "adviser",
    `/api/adviser/advice-records/${adviceRecordId!}/objectives`,
    "POST",
    { objectiveType: "retirement", description: "version test", priority: "primary" }
  );
  await expectStatus(res3a, 200, "objective POST succeeds on active record");

  const versionsAfterObjective = await db.query.adviceRecordVersions.findMany({
    where: eq(adviceRecordVersions.adviceRecordId, adviceRecordId!),
  });

  if (versionsAfterObjective.length === versionsBefore.length + 1) {
    pass("objective write creates version snapshot atomically");
  } else {
    fail(
      "objective write creates version snapshot atomically",
      `before: ${versionsBefore.length}, after: ${versionsAfterObjective.length}`
    );
  }

  // TODO: replace /issue-soa with the actual SOA issuance route from your codebase.
  // Enable this block after the SOA snapshot hook task ships.
  // If the route does not exist yet, leave this block commented so the script
  // still exits 0 during UI development.
  //
  // const res3b = await asRole(
  //   "adviser",
  //   `/api/adviser/advice-records/${adviceRecordId!}/issue-soa`, // ← replace this
  //   "POST"
  // );
  // const versionsAfterSOA = await db.query.adviceRecordVersions.findMany({
  //   where: eq(adviceRecordVersions.adviceRecordId, adviceRecordId!),
  // });
  // if (versionsAfterSOA.length > versionsAfterObjective.length) {
  //   pass("SOA issuance creates version snapshot");
  // } else {
  //   fail("SOA issuance creates version snapshot", "no new version row after SOA issuance");
  // }

  // ── 4. Append-only ─────────────────────────────────────────────────────────

  const noteRes = await asRole(
    "adviser",
    `/api/adviser/advice-records/${adviceRecordId!}/notes`,
    "POST",
    { noteText: "original note", noteType: "general", visibility: "adviser_only" }
  );
  const note = await noteRes.json();

  const res4a = await asRole("adviser", `/api/adviser/notes/${note.id}`, "PUT", {
    noteText: "edit attempt",
  });
  if (res4a.status === 405 || res4a.status === 404) {
    pass("PUT on adviserNotes rejected — append-only enforced");
  } else {
    fail("PUT on adviserNotes rejected", `got ${res4a.status}`);
  }

  const res4b = await asRole("adviser", `/api/adviser/notes/${note.id}`, "DELETE");
  if (res4b.status === 405 || res4b.status === 404) {
    pass("DELETE on adviserNotes rejected");
  } else {
    fail("DELETE on adviserNotes rejected", `got ${res4b.status}`);
  }

  // Schema inspection: adviserNotes must not have updatedAt column
  try {
    const col = await db.execute(
      `SELECT column_name FROM information_schema.columns
       WHERE table_name = 'adviser_notes' AND column_name = 'updated_at'`
    );
    const rows = Array.isArray(col) ? col : col.rows ?? [];
    if (rows.length === 0) {
      pass("adviserNotes has no updatedAt column");
    } else {
      fail("adviserNotes has no updatedAt column", "updatedAt column found — append-only violated");
    }
  } catch (e) {
    fail("adviserNotes schema inspection", String(e));
  }

  const note2Res = await asRole(
    "adviser",
    `/api/adviser/advice-records/${adviceRecordId!}/notes`,
    "POST",
    {
      noteText: "superseding note",
      noteType: "general",
      visibility: "adviser_only",
      previousNoteId: note.id,
    }
  );
  const note2 = await note2Res.json();
  if (note2.previousNoteId === note.id) {
    pass("previousNoteId chain is traversable");
  } else {
    fail("previousNoteId chain is traversable", `previousNoteId is ${note2.previousNoteId}`);
  }

  const obj1Res = await asRole(
    "adviser",
    `/api/adviser/advice-records/${adviceRecordId!}/objectives`,
    "POST",
    { objectiveType: "education", description: "original", priority: "secondary" }
  );
  const obj1 = await obj1Res.json();

  await asRole(
    "adviser",
    `/api/adviser/advice-records/${adviceRecordId!}/objectives`,
    "POST",
    {
      objectiveType: "education",
      description: "superseding",
      priority: "secondary",
      supersedes: obj1.id,
    }
  );

  const obj1Check = await db.query.clientObjectives.findFirst({
    where: eq(clientObjectives.id, obj1.id),
  });
  if (obj1Check?.supersededAt !== null && obj1Check?.supersededAt !== undefined) {
    pass("objective supersession sets supersededAt on prior row");
  } else {
    fail("objective supersession sets supersededAt on prior row", "supersededAt is null");
  }

  // ── 5. Audit log before/after ──────────────────────────────────────────────
  // Check auditLogs.resourceId column type in shared/schema.ts before running.
  // If integer: eq(auditLogs.resourceId, adviceRecordId)
  // If text:    eq(auditLogs.resourceId, String(adviceRecordId))
  // The line below uses String() — update if your schema uses integer.

  const recentLogs = await db.query.auditLogs.findMany({
    where: eq(auditLogs.resourceId, String(adviceRecordId!)), // ← verify type
    orderBy: desc(auditLogs.createdAt),
    limit: 10,
  });

  const logsWithBeforeAfter = recentLogs.filter(
    (l) =>
      l.metadata !== null &&
      typeof l.metadata === "object" &&
      "before" in (l.metadata as object) &&
      "after" in (l.metadata as object)
  );

  if (logsWithBeforeAfter.length > 0) {
    pass("plan writes produce audit logs with before/after state");
  } else {
    fail(
      "plan writes produce audit logs with before/after state",
      `${recentLogs.length} logs found, none have before/after. ` +
        `Check writeAuditLog helper is being called on all write paths.`
    );
  }

  // ── 6. No ledger side effects ──────────────────────────────────────────────

  const ledgerBefore = await db.query.ledgerEntries.findMany({
    where: eq(ledgerEntries.referenceType, "advice_record"),
  });

  await asRole(
    "adviser",
    `/api/adviser/advice-records/${adviceRecordId!}/notes`,
    "POST",
    { noteText: "ledger check note", noteType: "general", visibility: "adviser_only" }
  );

  const ledgerAfter = await db.query.ledgerEntries.findMany({
    where: eq(ledgerEntries.referenceType, "advice_record"),
  });

  if (ledgerAfter.length === ledgerBefore.length) {
    pass("no ledger entries created by planner writes");
  } else {
    fail(
      "no ledger entries created by planner writes",
      `before: ${ledgerBefore.length}, after: ${ledgerAfter.length}`
    );
  }

  // Wallet balance unchanged after planner write
  const walletBefore = await db.query.wallets.findFirst({
    where: eq(wallets.userId, /* clientUser.id */ 0),
  });

  await asRole(
    "adviser",
    `/api/adviser/advice-records/${adviceRecordId!}/objectives`,
    "POST",
    { objectiveType: "property", description: "wallet check", priority: "aspirational" }
  );

  const walletAfter = await db.query.wallets.findFirst({
    where: eq(wallets.userId, /* clientUser.id */ 0),
  });

  if (walletBefore?.balance === walletAfter?.balance) {
    pass("wallet balance not mutated by planner writes");
  } else {
    fail(
      "wallet balance not mutated by planner writes",
      `before: ${walletBefore?.balance}, after: ${walletAfter?.balance}`
    );
  }

  // ── 7. Document records ────────────────────────────────────────────────────

  const res7a = await asRole(
    "adviser",
    `/api/adviser/advice-records/${adviceRecordId!}/documents`,
    "POST",
    {
      documentType: "fact_find",
      fileName: "test-fact-find.pdf",
      storageKey: "test/fact-find-placeholder",
      description: "test upload",
    }
  );
  await expectStatus(res7a, 200, "document record created by adviser");

  const res7b = await asRole(
    "client",
    `/api/adviser/advice-records/${adviceRecordId!}/documents`,
    "POST",
    { documentType: "correspondence", fileName: "client.pdf", storageKey: "test/client" }
  );
  await expectStatus(res7b, 403, "client cannot upload documents");

  // ── Results ────────────────────────────────────────────────────────────────

  console.log("\n── Planner verification results ──\n");
  for (const r of results) {
    console.log(
      `${r.passed ? "PASS" : "FAIL"} ${r.name}${r.details ? ` — ${r.details}` : ""}`
    );
  }

  const failed = results.filter((r) => !r.passed);
  if (failed.length > 0) {
    console.error(`\n${failed.length} planner test(s) failed.`);
    process.exit(1);
  }

  console.log("\nALL PLANNER TESTS PASSED ✅");
}

run().catch((err) => {
  console.error("Planner verification script crashed:", err);
  process.exit(1);
});

Four things the developer must do before running — in order:
1. Confirm wallets is exported from shared/schema.ts
   (it is now imported — verify the export exists)

2. Check auditLogs.resourceId column type in shared/schema.ts
   Change eq(auditLogs.resourceId, String(adviceRecordId)) to
   eq(auditLogs.resourceId, adviceRecordId) if the column is integer

3. Replace the two /* clientUser.id */ 0 placeholders with the
   actual seeded client user id — same pattern as Gate B script

4. Uncomment the SOA block and replace /issue-soa with the real
   route name — only after the SOA snapshot hook task ships
Add to package.json:
json"test:planner": "tsx scripts/test-planner.ts"
Add to CI pipeline — runs after planner UI deploys, before next sprint is accepted:
yaml- name: Planner verification
  run: npm run test:planner
The script exits non-zero on any failure. The CI step blocks the next sprint if it does not exit 0.You said: Verified.Verified. This is correct, with one important caution: the script is not fully plug-and-play yet because it still contains seed placeholders.
Before dev runs it, they must fix:1. Add/confirm wallets export from shared/schema.ts
2. Replace /* clientUser.id */ 0 with actual seeded client id
3. Confirm auditLogs.resourceId type:
   - text → String(adviceRecordId)
   - integer → adviceRecordId
4. Replace route placeholders with actual routes
5. Keep SOA block commented until SOA snapshot hook ships
6. Copy seed/token creation from test-fee-deduction-gate-b.tsFinal sequencing is right:Write script now
Build audit logs
Build review lock
Build UI + docs + SOA hook
Enable SOA block
Run npm run test:planner
Block next sprint unless it exits 0No architecture issues remain. verify and cross checkShow more6:42 AMClaude responded: Verified.Verified. Everything is correct. This is the final clean state.
Cross-check verdict: confirmed accurate on all points. No conflicts, no missing items, no architectural issues.
Here is the precise verification of every item before this goes to dev.

  ::view-transition-group(*),
  ::view-transition-old(*),
  ::view-transition-new(*) {
    animation-duration: 0.25s;
    animation-timing-function: cubic-bezier(0.19, 1, 0.22, 1);
  }
VvisualizeVvisualize show_widgetThe state is clean. Nothing is open architecturally. The only remaining work before dev picks this up is the six pre-run fixes, all of which are mechanical — no design decisions required.
One thing worth flagging explicitly that the checklist marks as highest risk: the auditLogs.resourceId type check is the one fix that can produce a silent false-pass rather than an obvious failure. Every other placeholder either throws an error or produces a clearly wrong HTTP status. A wrong column type on the audit log query returns zero rows silently, test 5a passes, and you ship with a broken audit log standard and no signal that anything is wrong. Make that the first thing the developer checks, not the last.You said: Verified.Verified. This is the final clean state.
The only adjustment I’d make is prioritisation:First check: auditLogs.resourceId type
Reason: highest silent false-pass riskThen proceed:1. Confirm auditLogs.resourceId type
2. Confirm wallets export
3. Replace clientUser.id placeholders
4. Verify route names
5. Keep SOA block commented until hook ships
6. Copy Gate B seed/token pattern exactly
7. Add test:planner script
8. Add CI gateArchitecture is clean:No duplicate planner system
No ledger coupling
No payout execution
No open conflictsReady to build. verify and cross checkShow more6:44 AMClaude responded: Verified.Verified. This is correct and complete.
Final cross-check: confirmed clean. No conflicts, no missing items, no architectural issues.
The reordered pre-run checklist is the right call. Putting auditLogs.resourceId type first is correct because it is the only fix that fails silently. Every other fix produces an obvious error — wrong route gives 404, missing export throws at import time, wrong seed gives auth failure. The type mismatch gives zero rows and a green test. Silent false-passes are more dangerous than loud failures.
Everything else is verified as stated.

  ::view-transition-group(*),
  ::view-transition-old(*),
  ::view-transition-new(*) {
    animation-duration: 0.25s;
    animation-timing-function: cubic-bezier(0.19, 1, 0.22, 1);
  }
VvisualizeVvisualize show_widgetYou are out of free messages until 11:20 AMGet moreSonnet 4.6Claude is AI and can make mistakes. Ple