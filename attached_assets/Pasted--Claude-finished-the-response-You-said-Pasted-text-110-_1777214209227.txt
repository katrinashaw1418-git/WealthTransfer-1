
Claude finished the response
You said: Pasted text(110).

Next build: Gate B — Fee Deduction Execution Only proceed because: Ledger source of truth ✅ Reconciliation ✅ Posting safeguards ✅ Transaction safety tests ✅ Idempotency service extracted ✅ Replit prompt Proceed with Gate B — Fee Deduction Execution. Purpose: Enable admin-approved

pasted

Pasted text(110).txt
Document
verify and cross check. then let me know all the next builds 
Thought for a couple of seconds
Verified and cross-checked. The sequence is consistent and safe. Your next builds should be:
1. Gate B — Fee Deduction Execution
Build only after ledger, reconciliation, posting safeguards, transaction safety tests, and idempotency service are already merged.
Core build:

feeDeductionJournals table
POST /api/admin/fee-deductions/:id/deduct
POST /api/admin/fee-deductions/:id/reverse
GET /api/admin/fee-deduction-journals
update /admin/fees UI
Hard controls:

admin/compliance_admin only
approved deduction only
re-check active signed consent immediately before posting
block expired / withdrawn consent
block insufficient ledger balance
prevent duplicate journal
ledger debit only through ledger service
atomic writes + audit logs
no wallet mutation
no automatic deduction
no adviser-triggered deduction
no external payout / bank transfer
Gate B Verification + Safety Test Script
Before moving to reporting, build:
scripts/test-fee-deduction-gate-b.ts
Required tests:

non-admin cannot deduct
approved deduction posts once
duplicate deduction blocked
expired consent blocked
withdrawn consent blocked
insufficient balance blocked
ledger debit created
reversal ledger credit created
wallet balance not directly mutated
reconciliation clean after post/reversal Expected final output:
ALL GATE B FEE DEDUCTION TESTS PASSED ✅
Hard stop: if any test fails, do not proceed.
3. Fee Reconciliation + Payout Reporting
Only after Gate B tests pass.
Build:

GET /api/admin/fee-reconciliation
GET /api/admin/adviser-payouts
GET /api/admin/platform-fee-revenue
GET /api/admin/fee-exceptions
UI tabs in /admin/fees:

Reconciliation
Adviser Payouts
Platform Revenue
Exceptions
Important: this is reporting only.
Do not build yet:

bank transfer
external payout
automatic monthly sweep
commission tiers Final verified build order:
1. Gate B — Fee Deduction Execution
2. Gate B Verification + Safety Test Script
3. Fee Reconciliation + Payout Reporting
4. Hard stop before payout execution
anything to be added on wealth planner layer
Yes — add a Wealth Planner layer, but keep it planning + consent + records only, not advice execution.
Add after Fee Reporting
Wealth Planner Layer — Advice Records & Client Planning
Build:

wealthPlans table
wealthPlanRecommendations table
wealthPlanReviews table
clientObjectives / riskProfile snapshot
adviceDisclaimerAcknowledgements
planner notes + file upload records
admin/adviser UI: /adviser/planner and client view /wealth/plan
Core rules:

no product execution
no managed discretionary account logic
no automatic rebalancing
no “personal advice” wording unless AFSL/AR framework is ready
every recommendation must show general advice / information-only disclaimer
client must acknowledge disclaimer before viewing plan
keep immutable snapshots of:
objectives
risk profile
time horizon
liquidity needs
source of funds / wealth summary
selected model portfolio
adviser/planner notes Best build order
1. Gate B — Fee Deduction Execution
2. Gate B Verification + Safety Test Script
3. Fee Reconciliation + Payout Reporting
4. Wealth Planner Layer — Advice Records & Client Planning
5. Wealth Planner Verification Script
6. Only later: AFSL/AR-controlled advice execution workflow
Key thing to add: planner layer should create records and recommendations, not execute investments.
verify and cross check, and then let me know what to be fixed and what to be added on wealth planner layer

Show more
12:28 AM
Claude responded: I'll do a thorough cross-check of the entire build sequence and the wealth planner layer specifically.
I'll do a thorough cross-check of the entire build sequence and the wealth planner layer specifically.

Here's a thorough cross-check across the entire build sequence, then a detailed breakdown of what's missing or needs fixing in the wealth planner layer.



Cross-check verdict: The Gate B and Fee Reporting builds are internally consistent and safe. The wealth planner layer has real gaps that need fixing. Here's the full breakdown.

What needs to be fixed in the wealth planner layer
Critical gaps — missing tables and fields
1. wealthPlanVersions — you need immutable versioning The current spec says "keep immutable snapshots" but doesn't define how. You need a version table that locks a plan at a point in time. Without it, any edit to a wealthPlan silently overwrites the historical record.

Add:

ts
export const wealthPlanVersions = pgTable("wealth_plan_versions", {
  id: serial("id").primaryKey(),
  wealthPlanId: integer("wealth_plan_id").references(() => wealthPlans.id).notNull(),
  versionNumber: integer("version_number").notNull(),
  snapshotData: jsonb("snapshot_data").notNull(),
  // Full plan state at this point in time
  createdBy: integer("created_by").references(() => users.id),
  createdAt: timestamp("created_at").defaultNow(),
  changeReason: text("change_reason"),
});
2. riskProfileSnapshots needs to be its own table, not just a column A risk profile stored as a column on wealthPlans will get mutated when the plan is updated. It needs to be a separate append-only table:

ts
export const riskProfileSnapshots = pgTable("risk_profile_snapshots", {
  id: serial("id").primaryKey(),
  clientUserId: integer("client_user_id").references(() => users.id).notNull(),
  wealthPlanId: integer("wealth_plan_id").references(() => wealthPlans.id),
  riskTolerance: text("risk_tolerance").notNull(), // conservative | balanced | growth | aggressive
  timeHorizonYears: integer("time_horizon_years").notNull(),
  liquidityNeedsText: text("liquidity_needs_text"),
  sourceOfWealth: text("source_of_wealth"),
  capturedBy: integer("captured_by").references(() => users.id),
  capturedAt: timestamp("captured_at").defaultNow(),
});
3. clientObjectives is named but not defined The spec mentions it but gives no schema. At minimum you need:

ts
export const clientObjectives = pgTable("client_objectives", {
  id: serial("id").primaryKey(),
  clientUserId: integer("client_user_id").references(() => users.id).notNull(),
  wealthPlanId: integer("wealth_plan_id").references(() => wealthPlans.id),
  objectiveType: text("objective_type").notNull(), // retirement | education | property | estate | income | other
  targetAmount: decimal("target_amount", { precision: 15, scale: 2 }),
  targetDate: date("target_date"),
  priority: text("priority").notNull().default("primary"), // primary | secondary
  notes: text("notes"),
  createdBy: integer("created_by").references(() => users.id),
  createdAt: timestamp("created_at").defaultNow(),
});
4. adviceDisclaimerAcknowledgements needs strict fields This is a compliance record — it needs to capture exactly what disclaimer text was shown, not just that the client clicked "agree."

Add:

ts
export const adviceDisclaimerAcknowledgements = pgTable("advice_disclaimer_acknowledgements", {
  id: serial("id").primaryKey(),
  clientUserId: integer("client_user_id").references(() => users.id).notNull(),
  wealthPlanId: integer("wealth_plan_id").references(() => wealthPlans.id),
  disclaimerVersion: text("disclaimer_version").notNull(), // e.g. "general-advice-v1.2"
  disclaimerText: text("disclaimer_text").notNull(), // full text shown, locked at time of ack
  acknowledgedAt: timestamp("acknowledged_at").defaultNow(),
  ipAddress: text("ip_address"),
  userAgent: text("user_agent"),
});
Logic gaps — rules not defined
5. Disclaimer gate must be enforced server-side, not just UI The spec mentions "client must acknowledge disclaimer before viewing plan" but this needs to be enforced by the API. Every GET /wealth/plan/:id must verify a valid adviceDisclaimerAcknowledgement exists before returning plan data. A UI-only gate is bypassed by direct API calls.

6. wealthPlanRecommendations needs an explicit "general advice only" flag Every recommendation record must carry a adviceType field locked to "general" until AFSL/AR is in place. This prevents any recommendation from accidentally being classified or displayed as personal advice.

Add to the recommendations table:

ts
adviceType: text("advice_type").notNull().default("general"),
// Must be "general" — "personal" is blocked until AFSL/AR framework ready
generalAdviceDisclaimer: text("general_advice_disclaimer").notNull(),
// disclaimer text attached to this specific recommendation
7. Planner notes need audit trail fields Notes that can be edited need a lastEditedAt and lastEditedBy, not just createdAt. Better: make notes append-only (new row per edit, never update in place).

8. File upload records need to reference what they are The spec says "planner notes + file upload records" but doesn't define the file record schema. You need:

ts
export const wealthPlanDocuments = pgTable("wealth_plan_documents", {
  id: serial("id").primaryKey(),
  wealthPlanId: integer("wealth_plan_id").references(() => wealthPlans.id).notNull(),
  uploadedBy: integer("uploaded_by").references(() => users.id).notNull(),
  documentType: text("document_type").notNull(), // statement_of_advice | fact_find | risk_questionnaire | correspondence | other
  fileName: text("file_name").notNull(),
  storageKey: text("storage_key").notNull(),
  uploadedAt: timestamp("uploaded_at").defaultNow(),
});
What needs to be added (not just fixed)
9. wealthPlanReviews needs a formal review cycle schema Reviews should record who conducted the review, what changed, what the prior state was, and when the next review is due. Without this, regulatory audit trails are incomplete.

10. Role access rules need to be explicit The spec says advisers use /adviser/planner and clients use /wealth/plan but doesn't state the API-level access rules. You need to explicitly define:

Adviser can read/write their own clients' plans only
Client can read their own plan only (no write)
Admin can read all, write none (except flagging exceptions)
Compliance_admin can read all and flag plans for review
11. selectedModelPortfolio snapshot is mentioned but has no home The spec says to snapshot "selected model portfolio" but there's no modelPortfolios table referenced anywhere in the build sequence. Either define the table now or store it as a locked jsonb column on the risk profile snapshot. Don't leave it as an implied field — it will drift.

12. The verification script needs specific tests defined Unlike Gate B which lists all 10 tests explicitly, the wealth planner verification script is vague ("Wealth Planner Verification Script"). Before you build, define these tests:

Non-adviser cannot create a plan
Client cannot access another client's plan
Plan cannot be viewed without acknowledged disclaimer
Disclaimer acknowledgement records the full disclaimer text
Risk profile snapshot is immutable after capture
Recommendation adviceType cannot be set to "personal"
Plan version is created on every update
File upload creates an audit record
Admin can read but not mutate a plan
Reconciliation: no ledger side effects from planner actions
Summary: what to do before building
Issue	Action
wealthPlanVersions table missing	Add before building
riskProfileSnapshots as column not table	Fix — separate append-only table
clientObjectives schema undefined	Define schema
adviceDisclaimerAcknowledgements too thin	Add version, full text, IP, user-agent
Disclaimer gate UI-only	Enforce server-side on every plan API call
wealthPlanRecommendations no advice type lock	Add adviceType field locked to "general"
Planner notes no audit trail	Make append-only
File upload schema missing	Add wealthPlanDocuments table
wealthPlanReviews schema vague	Define review cycle fields
Role access rules implicit	Make explicit per-role per-endpoint
selectedModelPortfolio snapshot has no home	Assign to riskProfileSnapshots as jsonb or define modelPortfolios table
Verification script tests not listed	Define all 10 tests before building
The Gate B sequence is clean. The wealth planner layer needs these 12 items resolved in schema before any code is written.

You said: Verified and cross-checked.
Verified and cross-checked. Claude’s review is correct. The Gate B / fee sequence is clean, but the Wealth Planner layer needs more compliance-grade structure before build.
Add these before building:

1. wealthPlanVersions
2. riskProfileSnapshots
3. clientObjectives
4. adviceDisclaimerAcknowledgements with full disclaimer text/version/IP/user agent
5. wealthPlanDocuments
6. append-only plannerNotes
7. formal wealthPlanReviews
8. selectedModelPortfolio snapshot
9. API-level disclaimer gate
10. strict role access rules
11. adviceType locked to "general"
12. Wealth Planner verification script
Best corrected build order:

1. Gate B — Fee Deduction Execution
2. Gate B Verification + Safety Test Script
3. Fee Reconciliation + Payout Reporting
4. Wealth Planner Schema Foundation
5. Wealth Planner API + Server-Side Compliance Gates
6. Wealth Planner UI
7. Wealth Planner Verification Script
8. Hard stop before personal advice / execution / rebalancing
Main fix: do not build the Wealth Planner as a simple editable plan page. Build it as an append-only, versioned, disclaimer-gated planning record system.

Show more
12:32 AM
Claude responded: Confirmed.
Confirmed. Here is the full corrected build specification before any code is written.

Below is the complete corrected build specification — all schema, all API rules, all compliance gates, all verification tests — written as a single production-ready Replit prompt ready to hand off.

Wealth Planner — full corrected build specification
Build order (steps 4–8 of the full sequence)
4. Wealth Planner Schema Foundation
5. Wealth Planner API + Server-Side Compliance Gates
6. Wealth Planner UI
7. Wealth Planner Verification Script
8. Hard stop — no personal advice / execution / rebalancing until AFSL/AR framework
Step 4 — Wealth Planner Schema Foundation
Replit prompt

Proceed with Step 4 — Wealth Planner Schema Foundation.

Purpose: Build the append-only, versioned, disclaimer-gated schema for the wealth planner. No editable mutable plan records. Every write creates a new row or version. No advice execution, no rebalancing, no personal advice fields.

Add to shared/schema.ts:

ts
// Core plan record — one per client/adviser relationship
export const wealthPlans = pgTable("wealth_plans", {
  id: serial("id").primaryKey(),
  clientUserId: integer("client_user_id").references(() => users.id).notNull(),
  adviserUserId: integer("adviser_user_id").references(() => users.id).notNull(),
  status: text("status").notNull().default("active"),
  // active | closed | under_review
  createdBy: integer("created_by").references(() => users.id).notNull(),
  createdAt: timestamp("created_at").defaultNow(),
  closedAt: timestamp("closed_at"),
  closedBy: integer("closed_by").references(() => users.id),
  closureReason: text("closure_reason"),
}, (table) => ({
  uniqClientAdviser: uniqueIndex("wealth_plans_client_adviser_uidx")
    .on(table.clientUserId, table.adviserUserId),
}));

// Immutable version snapshot — created on every plan update, never overwritten
export const wealthPlanVersions = pgTable("wealth_plan_versions", {
  id: serial("id").primaryKey(),
  wealthPlanId: integer("wealth_plan_id").references(() => wealthPlans.id).notNull(),
  versionNumber: integer("version_number").notNull(),
  snapshotData: jsonb("snapshot_data").notNull(),
  // Full serialised state of the plan at this version
  changeReason: text("change_reason").notNull(),
  createdBy: integer("created_by").references(() => users.id).notNull(),
  createdAt: timestamp("created_at").defaultNow(),
}, (table) => ({
  uniqPlanVersion: uniqueIndex("wealth_plan_versions_plan_ver_uidx")
    .on(table.wealthPlanId, table.versionNumber),
}));

// Append-only risk profile — new row on every capture, never updated in place
export const riskProfileSnapshots = pgTable("risk_profile_snapshots", {
  id: serial("id").primaryKey(),
  clientUserId: integer("client_user_id").references(() => users.id).notNull(),
  wealthPlanId: integer("wealth_plan_id").references(() => wealthPlans.id).notNull(),
  riskTolerance: text("risk_tolerance").notNull(),
  // conservative | balanced | growth | aggressive
  timeHorizonYears: integer("time_horizon_years").notNull(),
  liquidityNeedsText: text("liquidity_needs_text").notNull(),
  sourceOfWealth: text("source_of_wealth").notNull(),
  employmentStatus: text("employment_status"),
  annualIncomeRange: text("annual_income_range"),
  netWorthRange: text("net_worth_range"),
  investmentExperience: text("investment_experience"),
  // none | limited | moderate | experienced
  selectedModelPortfolio: jsonb("selected_model_portfolio"),
  // Full locked snapshot of the model portfolio at time of capture
  // { name, assetAllocation, benchmarkIndex, lastReviewedAt }
  capturedBy: integer("captured_by").references(() => users.id).notNull(),
  capturedAt: timestamp("captured_at").defaultNow(),
});

// Client objectives — append-only, one row per objective entry
export const clientObjectives = pgTable("client_objectives", {
  id: serial("id").primaryKey(),
  clientUserId: integer("client_user_id").references(() => users.id).notNull(),
  wealthPlanId: integer("wealth_plan_id").references(() => wealthPlans.id).notNull(),
  objectiveType: text("objective_type").notNull(),
  // retirement | education | property | estate | income | liquidity | other
  description: text("description").notNull(),
  targetAmount: decimal("target_amount", { precision: 15, scale: 2 }),
  targetDate: date("target_date"),
  priority: text("priority").notNull().default("primary"),
  // primary | secondary | aspirational
  currency: text("currency").notNull().default("AUD"),
  createdBy: integer("created_by").references(() => users.id).notNull(),
  createdAt: timestamp("created_at").defaultNow(),
  supersededAt: timestamp("superseded_at"),
  // set when a newer objective replaces this one — never deleted
  supersededBy: integer("superseded_by"),
  // id of the newer clientObjectives row
});

// Disclaimer acknowledgements — compliance record, never deleted or updated
export const adviceDisclaimerAcknowledgements = pgTable("advice_disclaimer_acknowledgements", {
  id: serial("id").primaryKey(),
  clientUserId: integer("client_user_id").references(() => users.id).notNull(),
  wealthPlanId: integer("wealth_plan_id").references(() => wealthPlans.id).notNull(),
  disclaimerVersion: text("disclaimer_version").notNull(),
  // e.g. "general-advice-v1.0" — bump when text changes
  disclaimerText: text("disclaimer_text").notNull(),
  // Full text shown at time of acknowledgement — locked, not a reference
  acknowledgedAt: timestamp("acknowledged_at").defaultNow(),
  ipAddress: text("ip_address"),
  userAgent: text("user_agent"),
  sessionId: text("session_id"),
});

// Recommendations — general advice only, locked field, append-only
export const wealthPlanRecommendations = pgTable("wealth_plan_recommendations", {
  id: serial("id").primaryKey(),
  wealthPlanId: integer("wealth_plan_id").references(() => wealthPlans.id).notNull(),
  clientUserId: integer("client_user_id").references(() => users.id).notNull(),
  adviserUserId: integer("adviser_user_id").references(() => users.id).notNull(),
  adviceType: text("advice_type").notNull().default("general"),
  // LOCKED to "general" — personal blocked until AFSL/AR framework
  generalAdviceDisclaimer: text("general_advice_disclaimer").notNull(),
  // Full disclaimer text attached to this specific recommendation
  recommendationText: text("recommendation_text").notNull(),
  assetClassFocus: text("asset_class_focus"),
  // equities | fixed_income | property | cash | alternatives | mixed
  riskProfileSnapshotId: integer("risk_profile_snapshot_id")
    .references(() => riskProfileSnapshots.id).notNull(),
  // Recommendation must link to the risk profile at the time of advice
  status: text("status").notNull().default("draft"),
  // draft | presented | acknowledged | superseded
  presentedAt: timestamp("presented_at"),
  acknowledgedAt: timestamp("acknowledged_at"),
  supersededAt: timestamp("superseded_at"),
  createdBy: integer("created_by").references(() => users.id).notNull(),
  createdAt: timestamp("created_at").defaultNow(),
});

// Append-only planner notes — never updated in place
export const plannerNotes = pgTable("planner_notes", {
  id: serial("id").primaryKey(),
  wealthPlanId: integer("wealth_plan_id").references(() => wealthPlans.id).notNull(),
  noteText: text("note_text").notNull(),
  noteType: text("note_type").notNull().default("general"),
  // general | review | client_instruction | compliance | follow_up
  visibility: text("visibility").notNull().default("adviser_only"),
  // adviser_only | shared_with_client | admin_only
  createdBy: integer("created_by").references(() => users.id).notNull(),
  createdAt: timestamp("created_at").defaultNow(),
  // No updatedAt — append-only. New note supersedes old.
});

// Plan reviews — formal review cycle records
export const wealthPlanReviews = pgTable("wealth_plan_reviews", {
  id: serial("id").primaryKey(),
  wealthPlanId: integer("wealth_plan_id").references(() => wealthPlans.id).notNull(),
  reviewType: text("review_type").notNull().default("annual"),
  // annual | triggered | compliance | initial
  reviewedBy: integer("reviewed_by").references(() => users.id).notNull(),
  reviewedAt: timestamp("reviewed_at").defaultNow(),
  priorVersionId: integer("prior_version_id").references(() => wealthPlanVersions.id),
  newVersionId: integer("new_version_id").references(() => wealthPlanVersions.id),
  riskProfileSnapshotId: integer("risk_profile_snapshot_id")
    .references(() => riskProfileSnapshots.id),
  objectivesChanged: boolean("objectives_changed").notNull().default(false),
  riskProfileChanged: boolean("risk_profile_changed").notNull().default(false),
  recommendationsChanged: boolean("recommendations_changed").notNull().default(false),
  reviewSummary: text("review_summary").notNull(),
  nextReviewDue: date("next_review_due"),
  clientAcknowledgedAt: timestamp("client_acknowledged_at"),
  clientAcknowledgedBy: integer("client_acknowledged_by").references(() => users.id),
});

// Document records — file upload audit trail
export const wealthPlanDocuments = pgTable("wealth_plan_documents", {
  id: serial("id").primaryKey(),
  wealthPlanId: integer("wealth_plan_id").references(() => wealthPlans.id).notNull(),
  uploadedBy: integer("uploaded_by").references(() => users.id).notNull(),
  documentType: text("document_type").notNull(),
  // statement_of_advice | fact_find | risk_questionnaire |
  // correspondence | review_report | compliance_record | other
  fileName: text("file_name").notNull(),
  fileSizeBytes: integer("file_size_bytes"),
  mimeType: text("mime_type"),
  storageKey: text("storage_key").notNull(),
  uploadedAt: timestamp("uploaded_at").defaultNow(),
  description: text("description"),
});
Step 5 — Wealth Planner API + Server-Side Compliance Gates
Endpoints to build:

POST   /api/adviser/wealth-plans
GET    /api/adviser/wealth-plans/:id
POST   /api/adviser/wealth-plans/:id/risk-profiles
POST   /api/adviser/wealth-plans/:id/objectives
POST   /api/adviser/wealth-plans/:id/recommendations
POST   /api/adviser/wealth-plans/:id/notes
POST   /api/adviser/wealth-plans/:id/reviews
POST   /api/adviser/wealth-plans/:id/documents

GET    /api/client/wealth-plan
GET    /api/client/wealth-plan/recommendations

GET    /api/admin/wealth-plans
GET    /api/admin/wealth-plans/:id
POST   /api/admin/wealth-plans/:id/flag
Server-side compliance gates — enforced on every request, not just UI:

1. Disclaimer gate
   - GET /api/client/wealth-plan and GET /api/client/wealth-plan/recommendations
     must verify a valid adviceDisclaimerAcknowledgement exists
     for the requesting client and plan before returning any data.
   - If no acknowledgement: return 403 with { code: "DISCLAIMER_REQUIRED" }
   - Client must POST /api/client/wealth-plan/acknowledge-disclaimer first.
   - Disclaimer version must match the current active version in config.
   - If version mismatch: return 403 with { code: "DISCLAIMER_VERSION_OUTDATED" }

2. Role access rules
   - Adviser:
     can create and read plans for their own assigned clients only
     cannot read another adviser's plans
     cannot write to admin-only fields
     cannot set adviceType to anything other than "general"
   - Client:
     can read their own plan only (no write access to any plan table)
     cannot read planner notes with visibility = adviser_only or admin_only
   - Admin:
     can read all plans
     cannot mutate plan records directly
     can flag a plan for compliance review via POST /api/admin/wealth-plans/:id/flag
   - Compliance_admin:
     can read all plans and all notes
     can flag, can override review status
     cannot create or mutate plan records

3. Advice type lock
   - Every POST to /api/adviser/wealth-plans/:id/recommendations must
     reject any body where adviceType !== "general".
   - Return 400 with { code: "PERSONAL_ADVICE_BLOCKED",
     message: "Personal advice requires AFSL/AR framework. adviceType must be general." }
   - This is enforced server-side. UI selection is irrelevant.

4. Append-only enforcement
   - No PUT or PATCH on plannerNotes, riskProfileSnapshots, clientObjectives,
     adviceDisclaimerAcknowledgements, wealthPlanVersions, wealthPlanDocuments.
   - Only POST (new row) is permitted on these tables.
   - Objective updates: POST a new objective row, then set supersededAt on the old one
     in the same DB transaction.

5. Version on every plan update
   - Any write to wealthPlans, riskProfileSnapshots, clientObjectives, or
     wealthPlanRecommendations must atomically create a new wealthPlanVersions row
     with versionNumber = prior max + 1 and snapshotData = full plan state at that moment.

6. Audit log on every write
   - All plan writes must be recorded in the existing audit log with:
     userId, action, resourceType, resourceId, timestamp, ipAddress.
Step 6 — Wealth Planner UI
Routes to build:

/adviser/planner              adviser plan list
/adviser/planner/:planId      plan detail — tabs below
/wealth/plan                  client plan view (read-only)
Adviser plan detail tabs:

Overview        plan summary, status, next review date
Risk profile    current snapshot + history (append-only list, no edit button)
Objectives      objective list + add new (supersedes prior)
Recommendations list + add new recommendation (general advice only badge always visible)
Notes           append-only note feed, visibility tag on each note
Reviews         formal review history + initiate review
Documents       upload + list
Versions        full version history with snapshot diff view
UI rules:

- Every recommendations tab must show a persistent banner:
  "These are general advice recommendations only and do not constitute personal advice.
   You should consider your own circumstances before acting."
- Deduct button: does not exist anywhere in the planner UI.
- No "execute", "invest", "trade", "rebalance" buttons anywhere.
- Client /wealth/plan view shows only: overview, recommendations (if ack'd),
  objectives (read-only), review history.
- Client cannot see adviser-only notes.
- Disclaimer modal blocks /wealth/plan entirely until acknowledged.
  Modal must display the full disclaimer text, not a summary.
  Client must scroll to the bottom before the acknowledge button activates.
- Version history tab: adviser and admin only.
Step 7 — Wealth Planner Verification Script
Create scripts/test-wealth-planner.ts

Required tests — all 12 must pass before proceeding:

1.  PASS  non-adviser cannot create a wealth plan
2.  PASS  adviser cannot create a plan for another adviser's client
3.  PASS  client cannot view plan without acknowledged disclaimer
4.  PASS  disclaimer acknowledgement records full text, version, IP, user-agent
5.  PASS  outdated disclaimer version blocks plan access
6.  PASS  risk profile snapshot is immutable — no update permitted
7.  PASS  objective supersession creates new row and sets supersededAt on prior
8.  PASS  recommendation adviceType cannot be set to personal — server rejects
9.  PASS  plan update creates a new version snapshot atomically
10. PASS  planner note is append-only — no update or delete permitted
11. PASS  admin can read plan but cannot mutate it
12. PASS  no ledger side effects from any planner action

ALL WEALTH PLANNER TESTS PASSED ✅
Required final output format:

PASS non-adviser cannot create a wealth plan
PASS adviser cannot create plan for another adviser's client
PASS client blocked without acknowledged disclaimer
PASS disclaimer ack records full text, version, IP, user-agent
PASS outdated disclaimer version blocked
PASS risk profile snapshot immutable
PASS objective supersession correct
PASS personal advice type blocked server-side
PASS plan update creates version snapshot
PASS planner note append-only
PASS admin read-only enforced
PASS no ledger side effects from planner

ALL WEALTH PLANNER TESTS PASSED ✅
Hard stop — if any test fails, do not proceed to AFSL/AR framework.

Step 8 — Hard stop boundary
Do not build any of the following until a separate AFSL/AR build sequence is approved:

- Personal advice workflow
- Statement of Advice generation
- Managed discretionary account logic
- Automatic rebalancing
- Portfolio execution
- Trade instructions
- External fund manager integration
- Commission or ongoing advice fee automation tied to the planner
The planner is a planning record system only. It creates records, captures snapshots, and gates access. It does not execute anything.

Here is the full data model so you can see how every table connects before writing a line of code.



This is the complete corrected specification. A few things worth calling out before you build:

selectedModelPortfolio is stored as jsonb on riskProfileSnapshots, not as a foreign key to a modelPortfolios table. This is deliberate — the snapshot must lock the exact model portfolio data at the time of capture. If you later build a modelPortfolios table and a record changes, the snapshot must remain unchanged. A FK reference would leak updated data back into a historical record.

Objective supersession happens in one transaction. POST new objective row + set supersededAt on the prior row in the same DB write. Never delete the old row. The full history must be traversable for compliance review.

The disclaimer gate is enforced at the API layer on every request, not just at login. A client who acknowledges, logs out, and logs back in with a new session will be re-checked. If the disclaimer version in config has been bumped since their last acknowledgement, they get a 403 and must re-acknowledge before the plan is served.

The plannerNotes table has no updatedAt column by design. If you add one during migration, remove it. The absence of an update column is the signal to future developers that this table is append-only.

Verification script must exit non-zero if any test fails. Same pattern as Gate B. Do not proceed to AFSL/AR work until all 12 tests output PASS.






Claude is AI and can make mistakes. Please double-check responses.
